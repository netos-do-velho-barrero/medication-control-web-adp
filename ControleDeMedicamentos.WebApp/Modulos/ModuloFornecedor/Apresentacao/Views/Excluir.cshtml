@model ControleDeMedicamentos.WebApp.ModuloFornecedor.Apresentacao.ExcluirFornecedorViewModel

@{
    ViewBag.Titulo = "Excluir Fornecedor";
}

<div class="alert alert-warning">
    Tem certeza que deseja excluir o fornecedor <strong>@Model.Nome</strong>?
</div>

<dl class="row">
    <dt class="col-sm-2">Nome</dt>
    <dd class="col-sm-10">@Model.Nome</dd>

    <dt class="col-sm-2">CNPJ</dt>
    <dd class="col-sm-10">@Model.Cnpj</dd>
</dl>

<form asp-controller="Fornecedor" asp-action="ExcluirConfirmado" asp-route-id="@Model.Id" method="post">
    <div class="d-flex gap-2">
        <button type="submit" class="btn btn-danger">Excluir</button>
        <a class="btn btn-outline-secondary" asp-controller="Fornecedor" asp-action="Listar">Cancelar</a>
    </div>
</form>