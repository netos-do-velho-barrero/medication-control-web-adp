@model List<ControleDeMedicamentos.WebApp.ModuloFuncionario.Apresentacao.ListarFuncionarioViewModel>

@{
    ViewBag.Titulo = "Funcionários";
}

<div class="d-flex justify-content-end mb-3">
    <a class="btn btn-primary" asp-controller="Funcionario" asp-action="Cadastrar">Cadastrar Funcionário</a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">
        Nenhum funcionário cadastrado.
    </div>
}
else
{
    <table class="table table-striped table-hover align-middle">
        <thead>
            <tr>
                <th>Nome</th>
                <th>Telefone</th>
                <th>CPF</th>
                <th class="text-end">Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var funcionario in Model)
            {
                <tr>
                    <td>@funcionario.Nome</td>
                    <td>@funcionario.Telefone</td>
                    <td>@funcionario.Cpf</td>
                    <td class="text-end">
                        <a class="btn btn-outline-primary btn-sm" asp-controller="Funcionario" asp-action="Editar"
                           asp-route-id="@funcionario.Id">
                            Editar
                        </a>

                        <a class="btn btn-outline-danger btn-sm" asp-controller="Funcionario" asp-action="Excluir"
                           asp-route-id="@funcionario.Id">
                            Excluir
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
